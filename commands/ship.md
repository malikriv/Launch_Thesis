---
description: Run the BuilderKit ship-feature pipeline on a feature/bug/feedback request
argument-hint: <the request to ship>
---
Invoke the builderkit `ship-feature` skill with the user's request:
$ARGUMENTS. Read .builderkit/config.yaml first; if missing, run
/builderkit:setup before the pipeline.
